# Optional local lefthook configuration
# To enable this, add the following to your lefthook.yml:
#   - include(relative): lefthook-local.yml

# Uncomment to add pre-commit hook for automatic linting on staged files:
pre-commit:
    commands:
        lint:
            run: npm run lint:fix {staged_files}
            stage_fixed: true
